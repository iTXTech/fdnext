{
  "name": "fdnext",
  "private": true,
  "version": "1.1.0",
  "description": "FlashDetector TypeScript Next",
  "license": "AGPL-3.0-or-later",
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r --if-present typecheck",
    "server:dev": "pnpm -C packages/server dev",
    "server:start": "pnpm -C packages/server start",
    "fdbgen:build": "pnpm -C packages/fdbgen build",
    "fdbgen:generate": "pnpm -s tsx ./packages/fdbgen/src/cli.ts build",
    "fdbgen:crawl-mdb": "pnpm -s tsx ./packages/fdbgen/src/cli.ts crawl-mdb",
    "fdbgen:crawl-mdb-from-fbga": "pnpm -s tsx ./packages/fdbgen/src/cli.ts crawl-mdb-from-fbga",
    "baseline:gen": "pnpm -s tsx ./scripts/gen-baseline.ts",
    "baseline:check": "pnpm -C packages/compat-test test",
    "resources:update-micron-fbga": "pnpm -s tsx ./scripts/update-micron-fbga-codes.ts"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "tsx": "^4.21.0",
    "typescript": "^5.7.3"
  }
}
